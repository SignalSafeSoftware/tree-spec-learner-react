# Standalone export

Copied via `scripts/standalone/export-standalone-npm-package.sh`.

```bash
git init && git branch -M main
git add . && git commit -m "Export tree-spec-learner-react"
git remote add origin git@github.com:SignalSafeSoftware/tree-spec-learner-react.git
git push -u origin main
```
