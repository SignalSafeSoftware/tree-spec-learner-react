# Standalone export

This tree was generated by `scripts/export-standalone-npm-package.sh tree-spec-learner-react` from the DeliveryPlus monorepo.

```bash
git init
git branch -M main
git add .
git commit -m "Sync tree-spec-learner-react from DeliveryPlus"
git remote add origin git@github.com:SignalSafeSoftware/tree-spec-learner-react.git
git push -u origin main
```

See `RELEASING.md` for npm publish steps.
